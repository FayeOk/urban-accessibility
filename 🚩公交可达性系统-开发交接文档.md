# 公交可达性评价系统 — 开发交接文档（v2 · 最新版代码）

项目名：`urban-accessibility-rebuild`
文档日期：2026-09-13
说明：本文档基于 `关键源代码.zip` 重新生成，**替代此前基于旧版代码写的交接文档**。该压缩包是完整项目（约2G）中抽取出的关键源码，不含 `node_modules`、也不含前端运行时实际加载的 `public/data/output/*.geojson` 数据文件本体，见第 8 节说明。

---

## 1. 项目现状一句话总结

项目前端是 React + deck.gl 的可视化界面，所有分析结果以**离线预计算的静态 GeoJSON** 形式提供，由 `Back_end/data_process/` 下一整套 Python 脚本生成；前端本身**不做实时计算**，"分析设置"里的参数滑块目前是UI预留，尚未接后端重算接口。

---

## 2. 整体架构

```
┌───────────────────────────┐      离线批处理，人工运行       ┌──────────────────────────┐
│  原始数据                  │ ───────────────────────────▶  │ Back_end/data_process/   │
│  POI CSV / 人口栅格TIF /   │                                │ 一系列 Python 脚本         │
│  公交线网 SHP / 交通小区SHP │                                │ （见第5节脚本清单）         │
│  + 高德地图 API（等时圈/TCI）│                                └────────────┬─────────────┘
└───────────────────────────┘                                             │ 输出
                                                                            ▼
                                                          ┌──────────────────────────────┐
                                                          │ public/data/output/*.geojson  │
                                                          │ （前端运行时唯一数据来源，     │
                                                          │  本次压缩包中不含此目录实体）    │
                                                          └────────────┬──────────────────┘
                                                                       │ fetch()
                                                                       ▼
                                                          ┌──────────────────────────────┐
                                                          │ React 前端 (src/)              │
                                                          │ MapContainer 渲染 deck.gl 图层  │
                                                          │ AnalysisPanel 展示参数(未接后端)│
                                                          └──────────────────────────────┘
```

**关键点**：前端和后端数据处理之间**没有实时 API**，是"离线跑脚本 → 生成 GeoJSON → 前端读取静态文件"的模式，不是传统意义的前后端分离服务。

---

## 3. 前端目录结构

```
urban-accessibility-rebuild/
├── package.json                          # 依赖未变化（deck.gl / antd / turf 等）
├── config-overrides.js
├── vercel.json
├── fix_project.sh
├── src/
│   ├── App.jsx
│   ├── index.jsx
│   ├── pages/
│   │   └── MainPage.jsx                  # 状态从 geoData/showStops 扩展为 geoData/vis(8个图层开关)/tciSession
│   ├── components/
│   │   ├── Map/
│   │   │   └── MapContainer.jsx          # 从 92 行扩展到 ~1000 行，核心可视化逻辑全部在这里
│   │   ├── Sidebar/
│   │   │   ├── SidebarPanel.jsx          # 从2个Tab扩展为3个：数据管理/分析设置/综合评价
│   │   │   ├── DataUpload.jsx            # 逻辑基本不变，去除了 showStops 相关代码
│   │   │   ├── AnalysisPanel.jsx         # 从占位文字变为完整参数面板（381行）
│   │   │   └── CompositePanel.jsx        # 全新文件：综合可达性得分 + 供需失衡识别
│   │   ├── Algorithm/                    # 已建目录，目前为空
│   │   └── UI/                           # 已建目录，目前为空
│   └── styles/
└── Back_end/                             # 全新目录，见第5节
    └── data_process/
```

> `Algorithm/`、`UI/` 目录已经建好但里面没有文件，大概率是分析算法前端化或组件抽取的"坑位"，具体计划需与原开发者确认。

---

## 4. 前端功能模块说明

### 4.1 `MainPage.jsx` — 全局状态
统一管理一个 `vis` 对象，控制 8 类图层的显隐：
`demand`（需求密度）、`isochrone`（机会可达性）、`coverage`（网络覆盖度）、`tci`（公交竞争力）、`lines`（公交线路）、`stops`（公交站点）、`composite`（综合可达性）、`imbalance`（供需失衡）。
另有 `tciSession`（早高峰/平峰/晚高峰）控制 TCI 显示哪个时段。

### 4.2 `SidebarPanel.jsx` — 侧边栏三个 Tab
| Tab | 组件 | 内容 |
|---|---|---|
| 数据管理 | `DataUpload` | 用户自行上传 GeoJSON，独立图层叠加显示，不影响系统自带数据 |
| 分析设置 | `AnalysisPanel` | 需求密度/机会可达性/网络覆盖度/公交竞争力 四个指标的参数面板 |
| 综合评价 | `CompositePanel` | 综合可达性得分 + 供需失衡识别（象限分析）的图例说明 |

### 4.3 `AnalysisPanel.jsx` — 四指标参数面板
每个指标可展开查看/调整参数（平滑半径、衰减系数、POI权重、步行阈值、换乘惩罚等），并配有"重新计算"按钮。
⚠️ **该按钮目前点击后仅显示"待接入后端..."文字，不会真正触发重算**——所有参数调整目前只是 UI 演示，实际展示的图层数据仍是 Python 离线算好的静态结果，跟滑块数值无关。

### 4.4 `CompositePanel.jsx` — 综合评价
- "综合可达性得分"：熵权法合成需求密度/覆盖度/机会可达性三项指标。
- "供需失衡识别"：象限分析法，四类状态——供给不足 / 供需匹配 / 供给过剩 / 低效区域，各有独立配色。

### 4.5 `MapContainer.jsx` — 地图核心（重点扩展模块）
这是本次改动量最大的文件，新增内容包括：

1. **8 类 deck.gl 图层**：需求密度网格、机会可达性网格、网络覆盖度网格、TCI网格、交通小区边界、综合可达性网格、供需失衡网格、公交线路/站点，以及用户上传数据的独立图层。
2. **右上角工具栏**（`TopRightControls`）：线路开关、站点开关、CSV 数据下载面板（`DownloadPanel`）、地图样式切换（Dark/Street）。
3. **地址搜索框**（`SearchBox`）：调用高德地图 Geocoding API 定位地址并高亮最近站点。
4. **网格详情面板**（`GridDetailPanel`）：点击网格弹出该格的各项指标数值。
5. **交通小区等时圈对比**：针对"西城北部/朝阳西部/东南边缘区"3个样本小区，可切换早/平/晚高峰查看公交 vs 小汽车的等时圈范围叠加对比。
6. **CSV 导出**：按指标分组、逐字段勾选，导出成带表头的 CSV。

### 4.6 数据加载方式
所有系统自带图层通过 `fetch('/data/output/xxx.geojson')` 从**前端静态资源目录**加载，全部在组件挂载后异步请求，失败时静默忽略（`.catch(() => {})`），**没有加载失败的用户提示**，如果 `public/data/output/` 里文件缺失，对应图层会直接不显示，不会报错也不会提醒。

---

## 5. 后端数据处理管线（`Back_end/data_process/`）

这是一套**手动运行的 Python 离线脚本集合**，不是常驻服务。整理后的处理链路大致如下：

| 阶段 | 脚本 | 作用 | 输出 |
|---|---|---|---|
| 1. 需求密度 | `preprocess.py` / `preprocess_beijing.py` | 合并POI+人口栅格+公交线网，生成六边形需求网格 | `demand_grid_beijing.geojson`、`bus_lines_beijing.geojson`、`bus_stops_beijing.geojson` |
| 1a. 补充 | `add_poi_stats.py` | 给需求网格追加各类POI数量字段 | 更新 `demand_grid_beijing.geojson` |
| 1b. 补充 | `fix_population_calibration.py` | 只重算人口校准部分，不用重跑全流程 | 更新 `demand_grid_beijing.geojson` |
| 2. 机会可达性（等时圈） | `gen_isochrone_script.py` → 浏览器采集 → `isochrone_AmapJS.py` / `isochrone_fetch_puppeteer.js` → `isochrone_score_beijing.py` | 通过高德地图采集45分钟公交等时圈，评分每个网格可达的POI加权量 | `isochrone_grid_beijing.geojson` |
| 2a. 前端用等时圈轮廓 | `convert_isochrone_for_frontend.py` | 坐标系转换（GCJ-02→WGS-84） | `isochrone_polys.geojson` |
| 3. 网络覆盖度 | `coverage_beijing.py` / `Coverage.py` | 步行便捷度 + 线路多样性加权 | `coverage_grid_beijing.geojson` |
| 4. 公交竞争力(TCI) | `collect_tci_data.py`（采集）→ `compute_tci.py`（计算） | 东西城261网格 × 17个目的地，公交/小汽车出行时间比 | `tci_grid_beijing.geojson` |
| 5. 交通小区等时圈对比 | `compute_zone_centroids.py` → `collect_zone_isochrones.py` / `gen_zone_bus_isochrone.py` → `quick_isochrone.py` / `process_bus_isochrone.py` | 3个样本交通小区的公交/小汽车等时圈射线采样与边界提取 | `zone_isochrones.geojson` |
| 6. 综合评价 | （脚本未包含在本次压缩包中，`composite_xicheng.geojson` 为最终产物） | 熵权法综合得分 + 供需象限分类 | `composite_xicheng.geojson` |
| 辅助 | `run_step3_to_5.py` | 已有需求网格时跳过第1步，只重跑后续步骤 | — |
| 辅助 | `quick_isochrone.py`、`test.py`、`test2.py`、`plot_final_destinations.py` | 出图校验/坐标系测试等一次性脚本 | 根目录的几张 `.png` 校验图 |

**依赖安装**：
```bash
pip install transbigdata geopandas rasterio shapely scipy pandas numpy pyproj
```

---

## 6. ⚠️ 地理覆盖范围不一致（需重点确认）

四个指标 + 综合评价的**空间范围并不统一**，容易造成理解偏差：

| 指标 | 覆盖范围 |
|---|---|
| 需求密度 | 北京全市 16 区 |
| 网络覆盖度 | 北京全市 |
| 机会可达性（等时圈） | 仅 6 个核心区 |
| 公交竞争力 TCI | 仅东城+西城，261 个网格 |
| 综合可达性 / 供需失衡 | 仅西城区，261 个网格 |

也就是说，切到"综合评价"Tab 看到的分析结果范围比"需求密度"小得多。建议交接时向原开发者确认：这是数据采集成本限制导致的阶段性范围，还是最终产品就是聚焦西城/东西城的定稿范围，避免后续误解。

---

## 7. 已知问题与风险点

1. **两个API密钥硬编码在前端源码中**：
   - Mapbox token（比旧版本已轮换过一次，但仍是明文硬编码）
   - 高德地图 Key（`AMAP_KEY`，明文硬编码，用于搜索框的地址解析）
   建议都迁移到环境变量，并检查高德/Mapbox 后台是否已做域名白名单限制。

2. **分析参数滑块不生效**：`AnalysisPanel` 里所有参数目前只是前端展示，调整后不会触发真实重算，"重新计算"按钮点击后明确提示"待接入后端..."。如果给别人演示，容易被误认为是实时分析功能。

3. **Python 脚本中大量硬编码绝对路径**，例如：
   ```
   BASE = "/Users/aaa/Desktop/MajorThesis/System_build"
   ```
   这些路径只在原作者的 Mac 电脑上有效，换机器跑脚本前需要先全局替换成相对路径或命令行参数。

4. **代码变更未提交到 Git**：新版本 `git log` 显示最新提交仍是"优化地图加载速度和兼容性"，和上一版压缩包完全一致——说明 `AnalysisPanel`、`CompositePanel`、`Back_end/` 等本次看到的改动**都还停留在本地未提交状态**，交接时务必让原开发者先 `commit`/`push`，避免代码丢失。

5. **前端加载数据失败无提示**：`fetch('/data/output/xxx.geojson')` 出错时是 `.catch(() => {})` 静默吞掉，图层不出现但界面不会报错，排查"某个图层不显示"问题时容易忽略是数据文件缺失导致的。

6. **本次压缩包不含 `public/data/output/` 实际数据文件**（用户说明完整项目约2G，大概率就是这批 GeoJSON/原始数据体积较大），意味着仅凭这份源码**无法直接本地跑起完整效果**，需要额外拿到这批数据文件放入对应目录。

## 7. 已知问题与风险点

1. **两个 API 密钥硬编码在前端源码中**：
   - Mapbox token（比旧版本已轮换过一次，但仍是明文硬编码）
   - 高德地图 Key（`AMAP_KEY`，新增，明文硬编码，用于搜索框的地址解析）
   建议都迁移到环境变量，并检查高德/Mapbox 后台是否已做域名白名单限制。

2. **分析参数滑块不生效**：`AnalysisPanel` 里所有参数目前只是前端展示，调整后不会触发真实重算，"重新计算"按钮点击后明确提示"待接入后端..."。如果给别人演示，容易被误认为是实时分析功能。

3. **Python 脚本中大量硬编码绝对路径**，例如：
   ```
   BASE = "/Users/aaa/Desktop/MajorThesis/System_build"
   ```
   这些路径只在原作者的 Mac 电脑上有效，换机器跑脚本前需要先全局替换成相对路径或命令行参数。

4. **代码变更未提交到 Git**：新版本 `git log` 显示最新提交仍是"优化地图加载速度和兼容性"，和上一版压缩包完全一致——说明 `AnalysisPanel`、`CompositePanel`、`Back_end/` 等本次看到的改动**都还停留在本地未提交状态**，交接时务必让原开发者先 `commit`/`push`，避免代码丢失。

5. **前端加载数据失败无提示**：`fetch('/data/output/xxx.geojson')` 出错时是 `.catch(() => {})` 静默吞掉，图层不出现但界面不会报错，排查"某个图层不显示"问题时容易忽略是数据文件缺失导致的。

6. **本次压缩包不含 `public/data/output/` 实际数据文件**（用户说明完整项目约2G，大概率就是这批 GeoJSON/原始数据体积较大），意味着仅凭这份源码**无法直接本地跑起完整效果**，需要额外拿到这批数据文件放入对应目录。

7. **综合评价的生成脚本本次未包含**：`composite_xicheng.geojson` 是怎么从熵权法算出来的，对应的 Python 脚本没有在这次的压缩包里，需要额外找原开发者要。

---

## 8. 本次交接材料清单

**GitHub 仓库（`git@github.com:FayeOk/urban-accessibility.git`）里包含**：
- 完整前端源码（`src/`）
- 后端数据处理全部 Python/JS 脚本（`Back_end/data_process/`，仅脚本本身）
- 项目配置文件、`.gitignore`、`README.md`、本交接文档
- 体积较小的校验图片（`figure_composite.png` 等）

**仓库里不包含，需要开发者另行获取、手动放入本地项目目录的**（原因：单个文件超过 GitHub 100MB 硬性上限，或整体体积过大不适合放代码仓库）：

| 本地路径 | 内容 | 前端运行是否必须 |
|---|---|---|
| `public/data/output/` | 前端直接渲染用的全部 GeoJSON（约396M，其中 `bus_lines.geojson` 单个就有231M） | ✅ **必须**，缺了地图就是空白 |
| `data/` | 原始 POI/公交线网/等时圈等输入数据（shp、csv、geojson） | ❌ 仅重跑 Python 处理管线时需要 |
| `Back_end/landscan-global-2022-assets/` | 人口栅格 TIF 原始数据 | ❌ 仅重跑 Python 处理管线时需要 |
| `Back_end/data_process/北京区块数据.*`（shp等一整套） | 交通小区边界原始数据 | ❌ 仅重跑 Python 处理管线时需要 |
| `Back_end/data_process/output/` | Python 脚本跑出来的中间结果（内容和 `public/data/output/` 基本重复） | ❌ 一般不需要，除非要调试处理流程 |

> ⚠️ **原开发者请在交接前务必先把上表左列这几个文件夹/文件打包，上传到网盘或团队共享的云存储（阿里云盘/百度网盘/Google Drive/学校内部存储等均可），把下载链接填进下面第9节，再把文档发给接手的开发者**——这是目前唯一没有自动化的一步，需要人工做一次。

---

## 9. 数据获取与本地环境搭建（接手开发者照做即可）

### 9.1 获取代码
```bash
git clone git@github.com:FayeOk/urban-accessibility.git
cd urban-accessibility
```
如果没配置过 SSH 密钥导致 clone 失败，改用 HTTPS 方式：
```bash
git clone https://github.com/FayeOk/urban-accessibility.git
```

### 9.2 获取数据文件并放到正确位置
1. 从下面链接下载数据压缩包（**原开发者填写**）：
   `[ 在这里粘贴网盘/云存储下载链接 ]`
2. 解压后，按上一节表格里"本地路径"那一列，把对应文件夹**原样覆盖/放入**项目根目录下的同名路径。最起码要把 `public/data/output/` 放好，地图才有数据可显示；`data/`、`Back_end/landscan-global-2022-assets/`、`Back_end/data_process/北京区块数据.*` 只有在需要重新跑 Python 处理脚本、重新生成数据时才用得到，只看可视化效果可以先不放。
3. 放好后目录结构大致应该是：
   ```
   urban-accessibility-rebuild/
   ├── public/
   │   └── data/
   │       └── output/          ← 从网盘下载后放这里
   │           ├── bus_lines_beijing.geojson
   │           ├── demand_grid_beijing.geojson
   │           └── ...（其余geojson）
   ├── data/                    ← 需要重跑处理脚本时才放
   ├── Back_end/
   │   ├── data_process/
   │   │   └── 北京区块数据.shp 等  ← 需要重跑处理脚本时才放
   │   └── landscan-global-2022-assets/  ← 需要重跑处理脚本时才放
   └── src/ ...
   ```

### 9.3 安装依赖并启动前端
```bash
npm install --legacy-peer-deps
npm start
```
启动后浏览器会自动打开（或手动访问）`http://localhost:3000`。

**常见问题排查**：
- 打开是空白地图 / 图层都不显示 → 检查 `public/data/output/` 里的文件是否已经放齐，文件名有没有对不上（对照 9.2 步骤3的清单）。
- 启动报错、提示某个 `.css` 文件找不到 → 在项目根目录运行 `bash fix_project.sh`，它会自动清理缓存并补齐缺失的空样式文件，再重新 `npm start`。
- 地址搜索框、等时圈等依赖地图API的功能不工作 → 代码里的 Mapbox token / 高德 Key 是硬编码在 `MapContainer.jsx` 里的旧密钥，如果原开发者已经吊销或轮换过，需要找他要新的替换进去（详见第7节风险点1）。

### 9.4（可选）重新跑后端数据处理脚本
只有在需要重新生成/更新分析数据时才需要，日常查看可视化效果不需要做这一步：
```bash
pip install transbigdata geopandas rasterio shapely scipy pandas numpy pyproj
python Back_end/data_process/preprocess_beijing.py
```
运行前注意第7节提到的问题：脚本里的路径目前是原开发者电脑上的绝对路径（如 `/Users/aaa/Desktop/...`），需要先手动改成自己电脑上的实际路径。

---

## 10. 修改代码后，如何提交回原开发者的 GitHub 仓库

### 10.1 先确认自己有没有推送权限
这个仓库属于原开发者（`FayeOk`）个人账号。接手开发者如果不是仓库的协作者（collaborator），直接 `git push` 会被拒绝。有两种情况：

- **原开发者已经把你加为协作者**（GitHub仓库页面 → Settings → Collaborators 里能看到你的账号）：可以直接推送，跳到10.2。
- **还没加你**：需要先让原开发者去仓库的 Settings → Collaborators and teams 里，用你的 GitHub 用户名把你加进去并授予写权限；或者你自己先 Fork 一份到自己账号下修改，改完在 GitHub 网页上发起 Pull Request，由原开发者审核后合并（这种方式更适合"临时帮忙改一下"而不是长期共同开发）。

### 10.2 提交并推送修改
```bash
git status                     # 先看一眼改了哪些文件，确认没有把数据文件也改动/加进去
git add -A
git commit -m "简要描述这次改了什么"
git push origin main
```
如果 push 时提示远程有本地没有的新提交（比如其他人也改过），先合并一下再推：
```bash
git pull --rebase origin main
# 如果有冲突，按提示解决后：
git add <冲突文件>
git rebase --continue
git push origin main
```

### 10.3 需要特别注意的几点
- **不要把 9.2 步骤里放进来的数据文件夹提交上去**。它们已经在 `.gitignore` 里被排除了，正常情况下 `git status` 不会显示它们，但如果不小心新建/改动了 `.gitignore` 之外路径下的数据文件，提交前务必用 `git status` 检查一遍。
- 如果你新增了自己的调试脚本、截图、临时文件，尽量也养成加进 `.gitignore` 的习惯，保持仓库干净。
- 涉及到 Mapbox token / 高德 Key 的改动（比如迁移到 `.env`），记得同时提交一份 `.env.example`（只写变量名，不写真实密钥值），方便后来者知道需要配置哪些环境变量，真实密钥私下传递，不要提交进仓库。

---

## 11. ⚠️ 地理覆盖范围不一致（需重点确认，同第6节）

见第6节表格：四个指标 + 综合评价的空间范围不统一（有的是全市，有的只覆盖东西城或西城区），交接时务必向原开发者确认这是阶段性范围还是定稿范围。

---

## 12. 建议的交接确认清单（可直接发给原开发者）

- [ ] 把本地未提交的改动 `git commit` / `git push`（本文档撰写时已完成）
- [ ] 把 `public/data/output/`（必须）以及 `data/`、`Back_end/landscan-global-2022-assets/`、`Back_end/data_process/北京区块数据.*`（可选）打包上传网盘，把链接填进第9.2节
- [ ] 在 GitHub 仓库 Settings → Collaborators 里添加接手开发者的账号权限
- [ ] 提供"综合评价"熵权法计算脚本（未包含在当前仓库里）
- [ ] 说明地理范围不一致是否是最终定稿范围
- [ ] 说明 `AnalysisPanel` 参数面板后续是否要真正接入后端重算，还是仅作展示
- [ ] 提供 Mapbox / 高德 Key 的账号归属，便于后续轮换或迁移到环境变量
- [ ] 说明 `src/components/Algorithm/`、`src/components/UI/` 空目录的规划用途

---

*本文档基于代码静态分析及实际操作过程整理生成，具体以接手开发者实测结果为准。*